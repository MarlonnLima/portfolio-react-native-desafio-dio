import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: "#FFF",
    borderRadius: 25,
    padding: 16,
    alignItems: 'center',
    marginVertical: 10
  }
});